[tox]
envlist = py39
isolated_build = True
; skipdist=True
[testenv]
whitelist_externals =
    poetry
commands = 
    poetry install
    poetry run pytest {posargs} --import-mode=importlib -p no:warnings